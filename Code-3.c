#include <stdio.h>
int main()
{
    int a,b,mul;
    printf("Enter the two number which you want to multiply:\n");
    scanf("%d%d",&a,&b);
    mul=a*b;
    printf("Multiply of two number is %d",mul);
    getch();
}


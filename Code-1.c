#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter the two number which you want to add:\n");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("Sum of two number is %d",sum);
    getch();
}
